import React from 'react';

const Header: React.FC = () => {
    return (
        <header className="header">
            <h1>SafePass</h1>
            <p>Live Monitoring</p>
        </header>
    );
};

export default Header;